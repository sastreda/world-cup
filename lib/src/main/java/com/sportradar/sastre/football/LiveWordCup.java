package com.sportradar.sastre.football;
/*
 * This Java source file was generated by the Gradle 'init' task.
 */

import com.sportradar.sastre.football.model.Match;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

import java.util.ArrayList;
import java.util.List;

public class LiveWordCup {
    Logger logger = LogManager.getRootLogger();

    public List<Match> getSummary() {
        return summary;
    }

    private List<Match> summary = new ArrayList<>();
    public void addMatch(String homeTeam, String awayTeam) throws Exception {
        logger.debug("add new match between");
        if (summary.isEmpty()){
            logger.debug("adding first match to the board");
        }
        Match match = new Match(homeTeam,awayTeam);
        if (summary.contains(match)){
            throw new Exception("Match already exists");
        }
        summary.add(new Match(homeTeam,awayTeam));
        logger.debug("The game between {} and {} has already started", homeTeam, awayTeam);
    }

/**
    public void updateMatch(String homeTeam, int homeScore, String awayTeam, int awayScore) {
        validateTeamName();
        validateScore();
    }

     private void updateMatch(String homeTeam, int homeScore, String awayTeam, int awayScore, boolean fix) {
     validateTeamName();
     validateScore();
     }

    public void finishMatch(String homeTeam, String awayTeam) {
        validateTeamName();
        validateScore();
    }
    public List<T> getLiveScoreBoard() {
        validateTeamName();
        validateScore();
    }
    public List<T> getSummary() {
        validateTeamName();
        validateScore();
    }*/

    private void validateCountryIsNotPlaying(Country country) throws Exception{
        //summary
//filter the ones they contain country as
//match localname or away
    }
}
